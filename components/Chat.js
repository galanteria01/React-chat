import styled from "styled-components"

const Chat = ({id, users}) => {
    return (
        <Container>
            <p>Recipeint email</p>
        </Container>
    )
}

export default Chat


const Container = styled.div``;